
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const InteractionHistory = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Histórico de Interações</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Histórico completo de interações em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
