
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const LeadScoring = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Scoring de Leads</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Componente de Lead Scoring em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
