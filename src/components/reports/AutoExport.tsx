
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const AutoExport = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Exportação Automática</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de exportação automática em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
