
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const ExceptionWorkflow = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Workflow de Exceções</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Workflow para tratamento de exceções em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
