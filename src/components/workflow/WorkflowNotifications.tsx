
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const WorkflowNotifications = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Notificações de Workflow</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de notificações em tempo real em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
