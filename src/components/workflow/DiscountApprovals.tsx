
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const DiscountApprovals = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Aprovações de Desconto</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de aprovação de descontos em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
