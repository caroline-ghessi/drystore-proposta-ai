
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const AuditTrail = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Auditoria Completa</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de auditoria completa em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
