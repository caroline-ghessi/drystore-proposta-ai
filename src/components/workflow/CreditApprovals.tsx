
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const CreditApprovals = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Aprovação de Crédito</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de aprovação de crédito automática em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
