
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const FraudDetection = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Detecção de Riscos</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de detecção de fraudes em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
